
@model TravelAgency.Models.Model.DataDeleteTourVM
@{
    ViewBag.Title = "DeleteTour";
}

<div class="row justify-content-center ">
    <div class="col-3 justify-content-center text-center">
        @using (Html.BeginForm("DeleteTour", "Administrator", FormMethod.Post))
        {
            <div class="card text-white bg-dark mb-3  text-center ">
                <p class="card-header bg-danger">Удаление тура</p>
                <div class="card-body">
                    <div class="card-text">
                        <p>Количество туров:@Model.CountTour</p>

                        @Html.LabelFor(x => x.Id,"Введите Id:")
                        @Html.ValidationMessageFor(x => x.Id)
                        @Html.TextBoxFor(x => x.Id)
                        <p></p>
                        <button type="submit" id="more-details" class="btn btn-light">Удалить</button>
                    </div>
                </div>
            </div>
        }
    </div>
</div>


